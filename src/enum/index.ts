export enum StorageKey {
  TOKEN = "token",
  REFRESH_TOKEN = "refreshToken",
  USER_SALT = "userSalt",
}

export enum AppStatus {
  PENDING = "PENDING",
  PUBLISHED = "PUBLISHED",
}

export enum UserRole {
  ADMIN = "ADMIN",
  DEVELOPER = "DEVELOPER",
}
